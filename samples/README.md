## Quick Test (No dataset needed)

The repository includes a few sample WAV files in `samples/` not used to train the model so you can test the demo immediately:

```bash
python -m streamlit run app.py
